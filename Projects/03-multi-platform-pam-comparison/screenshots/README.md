# Screenshots to capture

1. `entra-pim-audit.png` — PIM Resource audit log showing a full activation cycle in Entra ID.
2. `okta-authn-policy.png` — the Okta Authentication Policy configuration requiring a possession factor.
3. `okta-stepup-prompt.png` — the step-up (Okta Verify) prompt triggered when signing in as a test user.

Redact tenant/org names before committing.
