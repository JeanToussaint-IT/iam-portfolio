# Screenshots to capture

Save each as you complete the corresponding phase, then embed the best ones in the project `README.md` under "Proof It Works."

1. `dynamic-group-membership.png` — the Post-Production dynamic group showing auto-populated members (Phase 2).
2. `check-access-scope.png` — `Check access` on `rg-post-production` confirming Reader/custom role landed at the right scope, not the subscription (Phase 3).
3. `ca-report-only-logs.png` — the Conditional Access tab in Sign-in logs showing all three policies evaluated in Report-only mode (Phase 4).
4. `pim-audit-history.png` — the full PIM Audit history chain: request → approval → activation → expiry (Phase 5).

Redact tenant name, real emails, and any subscription IDs you don't want public before committing.
