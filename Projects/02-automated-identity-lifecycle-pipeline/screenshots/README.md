# Screenshots to capture

1. `memberof-before-after.png` — `Get-MgUserMemberOf` output before and after a leaver run, side by side.
2. `idempotency-two-runs.png` — the script run twice against unchanged data; the second run showing only SKIP entries.
3. `mover-revoke-log.png` — audit log excerpt showing a MOVER-REVOKE entry (stale group removed).
4. Optional but strongest: a 3-5 minute screen-capture demo of a live run, linked from the project README instead of a static image.

Redact tenant name and any real identifiers before committing.
